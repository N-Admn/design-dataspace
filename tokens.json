{
  "$meta": {
    "name": "CivicDataSpace Design Tokens",
    "description": "Canonical, machine-readable source of truth for CivicDataSpace design tokens. Values reflect the WCAG 2.2 AA color and typography remediation (accessibility audit, 2026-09-07) merged on top of the CSS-audited baseline. All colors are solid hex — where the baseline used oklch(), the hex is the exact OKLab→sRGB conversion. The Tailwind v4 theme in src/generated/tokens.css is GENERATED from this file via `npm run gen:tokens`; do not hand-edit that file. design-system.md is also generated from this file.",
    "tailwind": "v4 — @theme inline lives in src/generated/tokens.css",
    "generatedArtifacts": ["src/generated/tokens.css", "design-system.md"]
  },

  "color": {
    "$type": "color",
    "$description": "Base + brand + semantic color roles. Each key K produces the CSS custom property --K (kebab-cased) in :root and, unless listed in the generator's root-only set (ringOnDark, borderStrong), the Tailwind utility color --color-K via @theme inline.",
    "background": "#ffffff",
    "foreground": "#0a0a0a",
    "card": "#ffffff",
    "cardForeground": "#0a0a0a",
    "popover": "#ffffff",
    "popoverForeground": "#0a0a0a",
    "primary": "#0b3865",
    "primaryForeground": "#ffffff",
    "secondary": "#f4f4f5",
    "secondaryForeground": "#18181b",
    "muted": "#f5f5f5",
    "mutedForeground": "#6b6b6b",
    "accent": "#fdb557",
    "accentForeground": "#0b3865",
    "destructive": "#e7000b",
    "destructiveText": "#d12222",
    "destructiveForeground": "#ffffff",
    "success": "#46a758",
    "successForeground": "#ffffff",
    "successText": "#317f37",
    "warning": "#ffc53d",
    "warningForeground": "#0b3865",
    "border": "#e5e5e5",
    "input": "#8c8c8c",
    "ring": "#0b3865",
    "ringOnDark": "#ffffff",
    "controlHover": "#e4e4e6",
    "controlActive": "#d8d8db",
    "borderStrong": "#727272"
  },

  "chart": {
    "$type": "color",
    "$description": "Categorical data-viz palette — amber-to-brown ramp, each stop ≥ 3:1 on white. Key N produces --chart-N and --color-chart-N. chart-1 no longer aliases accent.",
    "1": "#d27802",
    "2": "#b46702",
    "3": "#a05b02",
    "4": "#8b5002",
    "5": "#7c4701"
  },

  "sidebar": {
    "$type": "color",
    "$description": "Sidebar surface scope. `DEFAULT` produces --sidebar / --color-sidebar; other keys produce --sidebar-<key> / --color-sidebar-<key>. Still unused by ContributorSidebar.tsx.",
    "DEFAULT": "#fafafa",
    "foreground": "#0a0a0a",
    "primary": "#0b3865",
    "primaryForeground": "#ffffff",
    "accent": "#fdb557",
    "accentForeground": "#0b3865",
    "border": "#e5e5e5",
    "ring": "#0b3865"
  },

  "surface": {
    "$type": "color",
    "$description": "App-chrome surfaces that sit outside the shadcn base set. Key K produces --<kebab(K)> and --color-<kebab(K)>. workspaceHeroFrom/To are the two stops of the Dashboard 'My Workspace' panel wash — compose the gradient inline: linear-gradient(252deg, var(--workspace-hero-from) 0%, var(--workspace-hero-to) 97.53%).",
    "pageBackground": "#f3f5f8",
    "headerBackground": "#0b2540",
    "breadcrumbBackground": "#fdb557",
    "workspaceHeroFrom": "#f4f5f8",
    "workspaceHeroTo": "#d3e9ff"
  },

  "font": {
    "$type": "fontFamily",
    "$description": "Font stacks. Key K produces --font-K in :root and the Tailwind --font-K theme key. Inter is loaded at weights 400–700 (300 dropped); JetBrains Mono at 400–500.",
    "sans": "'Inter', system-ui, sans-serif",
    "mono": "'JetBrains Mono', monospace"
  },

  "radius": {
    "$type": "dimension",
    "$description": "Border-radius scale. `DEFAULT` produces the --radius base custom property only (no Tailwind key, matching current implementation). sm/md/lg/xl produce --radius-<key> in :root and the Tailwind --radius-<key> theme key, and are expressed relative to --radius so the scale stays proportional.",
    "DEFAULT": "0.625rem",
    "sm": "calc(var(--radius) - 2px)",
    "md": "var(--radius)",
    "lg": "calc(var(--radius) + 4px)",
    "xl": "calc(var(--radius) + 8px)"
  }
}
